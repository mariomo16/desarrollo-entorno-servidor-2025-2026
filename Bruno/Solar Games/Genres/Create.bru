meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/genres
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  name: Soulslike
}

settings {
  encodeUrl: true
  timeout: 0
}
