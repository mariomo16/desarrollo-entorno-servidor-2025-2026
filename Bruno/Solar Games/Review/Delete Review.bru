meta {
  name: Delete Review
  type: http
  seq: 5
}

delete {
  url: {{BASE_URL}}/reviews/{{id}}
  body: none
  auth: inherit
}

vars:pre-request {
  id: 9
}

settings {
  encodeUrl: true
  timeout: 0
}
