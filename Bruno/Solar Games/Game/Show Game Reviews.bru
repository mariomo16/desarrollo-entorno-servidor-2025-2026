meta {
  name: Show Game Reviews
  type: http
  seq: 8
}

get {
  url: {{BASE_URL}}/games/{{id}}/reviews
  body: none
  auth: inherit
}

params:query {
  ~include: genre,reviews
}

vars:pre-request {
  id: 3
}

settings {
  encodeUrl: true
  timeout: 0
}
