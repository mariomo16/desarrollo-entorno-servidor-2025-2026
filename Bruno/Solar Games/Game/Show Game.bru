meta {
  name: Show Game
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/games/{{id}}?include=genre,reviews
  body: none
  auth: inherit
}

params:query {
  include: genre,reviews
}

vars:pre-request {
  id: 3
}

settings {
  encodeUrl: true
  timeout: 0
}
