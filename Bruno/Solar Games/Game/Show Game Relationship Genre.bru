meta {
  name: Show Game Relationship Genre
  type: http
  seq: 7
}

get {
  url: {{BASE_URL}}/games/{{id}}/relationships/genre
  body: none
  auth: inherit
}

params:query {
  ~include: genre,reviews
}

vars:pre-request {
  id: 3
}

settings {
  encodeUrl: true
  timeout: 0
}
